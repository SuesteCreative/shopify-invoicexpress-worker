node_modules
.wrangler
dist
.env
.env.*
!.env.example

.dev.vars*
!.dev.vars.example

# D1 backups (contain PII, keep local only)
backups/
*.sql
!migrations/*.sql
!schema.sql

# Migration script output (contains PII)
rioko-next/scripts/out/
